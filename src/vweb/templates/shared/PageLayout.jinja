{#def
    title="",
    body_attrs="",
    head_extra="",
    scripts_extra="",
    hide_chrome=False
#}

<!DOCTYPE html>
<html lang="en" data-theme="valentina-light">
  <head>
    <script src="{{ static_url('js/theme.js') }}"></script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Raleway:ital,wght@0,100..900;1,100..900&family=Roboto+Mono:ital,wght@0,100..700;1,100..700&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap"
          rel="stylesheet">

    <link rel="stylesheet" href="{{ static_url('css/style.css') }}">
    <link rel="icon"
          href="{{ static_url('img/favicon.svg') }}"
          type="image/svg+xml">
    <link rel="icon"
          href="{{ static_url('img/favicon-32x32.png') }}"
          type="image/png"
          sizes="32x32">
    <link rel="apple-touch-icon"
          href="{{ static_url('img/apple-touch-icon.png') }}"
          type="image/png"
          sizes="180x180">
    {{ head_extra }}
    <title>{{ app_name }}
      {% if title %}: {{ title }}{% endif %}
    </title>
  </head>
  {# Only mint a CSRF token for logged-in users. Calling csrf_token() writes to
     the session, which persists a Redis key; anonymous visitors have no HTMX
     mutations to protect, so skipping it keeps their session empty. #}
  {# djlint:off #}
  <body {% if is_authenticated() %}hx-headers='{"X-CSRFToken": "{{ csrf_token() }}"}'{% endif %}
        {{ body_attrs }}
        class="bg-base-200">
  {# djlint:on #}
    <div class="flex flex-col min-h-screen">
      {% if not hide_chrome %}<shared.layout.GlobalHeader />{% endif %}

      <main class="flex-1">
        <shared.layout.FlashMessage />
        {% if hide_chrome %}
          {{ content }}
        {% else %}
          <div class="p-4 gap-4">{{ content }}</div>
        {% endif %}
      </main>

      {% if not hide_chrome %}<shared.layout.GlobalFooter />{% endif %}
    </div>

    {% if global_context() and can_manage_campaign() %}
      <shared.layout.CampaignCreateModal />
    {% endif %}

    {# ------------------ Bottom of the page ------------------ #}
    <script src="https://unpkg.com/htmx.org@2.0.4"
            integrity="sha384-HGfztofotfshcF7+8n44JQL2oJmowVChPTg48S+jvZoztPfvwD79OC/LTtG6dMp+"
            crossorigin="anonymous"></script>
    <script defer src="https://unpkg.com/alpinejs@3/dist/cdn.min.js"></script>
    <script src="https://kit.fontawesome.com/29b83b145f.js"
            crossorigin="anonymous"></script>

    {% if is_approved_user() %}
      <script src="{{ static_url('js/dice-tumble.js') }}"></script>
      <script src="{{ static_url('js/section-nav.js') }}"></script>
    {% endif %}

    {{ scripts_extra }}
  </body>
</html>
